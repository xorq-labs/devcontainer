# Paths under the main worktree to symlink into new worktrees.
# Format: see lib/list-file.sh.
#
# Note: `.claude` is always symlinked by setup-worktree itself (audit
# logs and session captures are devcontainer infrastructure, not
# project-specific) — don't list it here.
#
# `ci/ibis-testing-data` is backed by test fixture data; this symlink lets
# sibling worktrees resolve into the same directory.
ci/ibis-testing-data
