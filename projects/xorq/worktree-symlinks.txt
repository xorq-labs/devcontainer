# Paths under the main worktree to symlink into new worktrees.
# Format: see lib/list-file.sh.
#
# Note: `.claude` is handled by setup-worktree itself — it makes a real
# .claude directory and symlinks only container-audit/ and
# container-sessions/ from the main checkout (audit logs and session
# captures are devcontainer infrastructure, not project-specific) — so
# don't list `.claude` or those subdirs here.
#
# `ci/ibis-testing-data` is backed by a docker named volume mounted at
# the main worktree's path (see compose.override.yml + external-volumes.txt);
# this symlink lets sibling worktrees resolve into that same directory.
ci/ibis-testing-data
