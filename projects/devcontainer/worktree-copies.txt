# Paths under the main worktree to copy (not symlink) into new worktrees.
# Format: see lib/list-file.sh. Patterns are interpreted as
# shell globs (`*`, `?`, `[...]`) by setup-worktree; shell substitutions
# (`$(...)`, backticks, `~`, `$VAR`) are not. Entries must not contain spaces.
.envrcs/.envrc.user
.envrcs/.envrc.secrets
# Dotenv fragments a .envrc.user may `dotenv_if_exists` — silently empty in a
# worktree if not copied alongside it. Does not re-match .envrc.*.
.envrcs/.env.*
project.env
